# Day 1 演示脚本

1. 展示 README 的模拟数据声明。
2. 运行 `python -m app.main --reset`，确认答案与引用来自测试文本。
3. 启动 Streamlit，输入同一问题并展开引用。
4. 展示 `docs/architecture.md` 的 8 环节数据流。
5. 说明当前 hash embedding 和回答测试替身仅用于基线，不能当作效果证据。
