# Day 1 验收记录

## 环境

- 执行器：Python 3.12（本机未安装计划建议的 3.11）
- 依赖：以 `requirements.lock.txt` 的实际安装结果为准
- LLM：默认测试替身；不读取、不提交真实密钥
- embedding：确定性本地 hash embedding，仅验证链路
- 向量库：Chroma 本地持久化
- UI：Streamlit

## 验收清单

- [x] 一条基线问答真实跑通并保存 JSON 结果
- [x] Streamlit 页面可访问并保存截图
- [x] 依赖可复现，密钥未入库
- [x] 架构图包含 8 个关键环节
- [x] 当前自有代码和参考来源缺口已如实记录
- [x] 请求/响应字段与错误码已经冻结
- [x] 本地 Git 首次提交
- [ ] GitHub 远程仓库与在线看板（缺少外部仓库信息/授权）
- [ ] 参考脚手架许可证核对（未提供参考 URL/提交号）
- [ ] 在线模型返回（Day 1 已按计划选择测试替身；需要生成效果时再接入）

## 基线问题

- 文档：`data/raw_docs/baseline_demo.txt`
- 问题：`维护窗口是什么时间？`
- 预期：回答和引用中可回查“每周三上午十点”
- 真实结果：`data/results/day1-baseline.json`
- 截图：`docs/evidence/day1-streamlit.png`

## Day 1 复盘

- 今天真实完成：本地仓库、依赖环境、8 环节 RAG 基线、UI、契约、文档和测试。
- 对应证据：Git commit、锁定文件、测试输出、JSON、UI 截图。
- 未完成及原因：GitHub/参考许可证/在线模型验证均缺少安全或来源条件。
- 今天发现的一个失败：计划建议 Python 3.11，但本机仅有 3.12。
- 根因判断：本机解释器版本差异。
- 处理：将项目约束固定为 Python 3.12，并用实际安装结果验证。
- 安装阶段真实失败：setuptools 将 `app/ui/data` 误判为多个顶层包，拒绝 editable build。
- 当时修复：在 `pyproject.toml` 显式只发现 `app*` 包后重新安装并通过测试。
- 后续审计：项目不发布 Python 包，已删除 editable build 与 `pyproject.toml`，从根源移除这段无用配置。
- 检索阶段真实问题：首轮中文 hash 分词把整段中文当成单个 token，相关问题距离为 1.0。
- 修复与复测：改为逐个中文字符与英文词分词，重新建库后相关问题距离下降；该策略仍只用于基线连线。
- 明天第一件事：制作明确标注为模拟数据的制造文档包。
- 是否需要砍 P1/P2：不影响；继续保持 P1 延后、P2 不做。
- 当前可对外表述：Day 1 基线已跑通；语义检索质量与在线模型效果仍不可写。
